# 多 Agent 个人管理工具 — 产品需求文档（PRD）

- 版本：v0.1（原型期）
- 日期：2026-07
- 状态：待评审
- 技术栈：CrewAI + DeepSeek（LLM）+ Python CLI

---

## 1. 第一性原理分析

### 1.1 回到本质：个人管理的本质问题是什么

剥掉所有工具的外衣，个人管理只有三件事：

1. **记录**：把脑子里的信息（日程、待办、开销、想法）可靠地外部化，防止遗忘。
2. **检索**：在需要的时候，能快速、准确地找回这些信息。
3. **提醒与行动**：在正确的时间点，信息主动回到你面前，推动行动。

现有工具（日历、待办 App、记账 App、笔记软件）各自解决了单点问题，但带来一个新的本质矛盾：

> **信息是统一的，工具是割裂的。**

用户说一句"明天下午三点开产品会，记得提醒我，会上要讨论预算，先把上个月差旅费汇总一下"——这在人的脑子里是**一句话、一个意图**，却需要在 3 个 App 里切换、按 3 套规则录入。工具的形态强迫用户做"意图拆解"这件本应由机器做的事。

### 1.2 为什么"多 Agent"是正确解，而不是功能堆砌

第一性原理推导：

- 大语言模型已经能可靠地完成**自然语言 → 结构化意图**的转换，这消除了"按各 App 规则录入"的必要性。
- 但单个 Agent 包揽所有领域会遇到：提示词臃肿、工具列表过长导致选择错误、领域逻辑互相污染。**专业化分工是降低复杂度的通用手段**——这和人类组织演化的逻辑一致。
- 因此架构收敛为：**一个理解意图的协调者 + N 个各管一域的执行者**。协调者负责"拆"，专职 Agent 负责"做"。

这正是调研中 CrewAI 的 Hierarchical 模式（Manager 自动委派）所提供的原生能力，也是选型它的核心依据。

### 1.3 一句话产品定义

> 用户用一句自然语言表达意图，系统自动拆解、分派给对应领域的专职 Agent 执行，并汇总结果——**让工具适应人，而不是人适应工具。**

---

## 2. 目标与非目标

### 2.1 本期目标（v0.1 原型）

- 验证核心价值假设：**单句自然语言输入 → 正确的多 Agent 分派 → 可靠执行**。
- 跑通 4 个领域：日程、待办、记账、笔记。
- 数据本地 JSON 持久化，零外部服务依赖（除 LLM API）。

### 2.2 成功衡量标准

| 指标 | 目标值 |
| --- | --- |
| 意图分派准确率（20 条测试指令） | ≥ 90% |
| 单条指令端到端响应时间 | ≤ 30 秒 |
| 跨领域复合指令（一句话含 2 个以上意图）正确处理 | 支持 |
| 数据持久化可靠性 | 重启后数据不丢失 |

### 2.3 明确的非目标（本期不做）

- ❌ Web UI / 移动 App（本期仅 CLI）
- ❌ 主动提醒推送（需要常驻进程和通知通道，v0.2 再说）
- ❌ 多用户、云同步、账号体系
- ❌ 与外部日历（Google Calendar 等）真实集成
- ❌ 数据库（JSON 文件够用，SQLite 留作演进项）

---

## 3. 用户与场景

### 3.1 目标用户

初期用户即开发者本人（personal tool），因此 PRD 的场景全部来自"自己会每天真实使用"的标准，不臆想他人需求。

### 3.2 核心用户故事

| # | 场景 | 输入示例 | 期望行为 |
| --- | --- | --- | --- |
| S1 | 记录日程 | "明天下午三点和张三开产品评审会" | 日程 Agent 存入事件，返回确认（含解析出的日期时间） |
| S2 | 查询日程 | "我这周有什么安排？" | 日程 Agent 列出本周事件 |
| S3 | 记录待办 | "提醒我给房东交房租" | 待办 Agent 存入待办 |
| S4 | 记账 | "午饭花了 35，打车 22" | 记账 Agent 拆成两笔记账，分类为餐饮/交通 |
| S5 | 账目查询 | "这个月吃饭一共花了多少？" | 记账 Agent 按分类汇总当月 |
| S6 | 记笔记 | "记一下：CrewAI 的 Hierarchical 模式适合这个项目的分派逻辑" | 笔记 Agent 存入笔记，可带标签 |
| S7 | **复合指令** | "明天下午三点开会，顺便记一笔今天午饭 35" | Manager 拆解为日程 + 记账两个子任务，分派给两个 Agent，汇总两个结果 |

S7 是本产品的**灵魂场景**——单点记录任何 App 都能做，跨域一句话处理才是多 Agent 的不可替代价值。

---

## 4. 产品架构与功能需求

### 4.1 总体架构

```
用户（终端自然语言输入）
        │
        ▼
┌──────────────────────┐
│   Manager Agent      │  协调者：意图理解、任务拆解、委派、结果汇总
│   （Hierarchical      │
│    流程自动指派）      │
└─────────┬────────────┘
          │ 委派
   ┌──────┼──────────┬──────────┐
   ▼      ▼          ▼          ▼
 日程    待办       记账       笔记
 Agent  Agent     Agent     Agent
   │      │          │          │
   └──────┴── 各自专属工具 ─────┘
                     │
              data/*.json 本地持久化
```

### 4.2 功能需求明细

**FR-1 Manager Agent（协调者）**
- FR-1.1 接收用户原始输入，识别其中包含的一个或多个意图
- FR-1.2 将子任务委派给对应领域的专职 Agent（CrewAI Hierarchical 模式原生能力）
- FR-1.3 汇总各 Agent 的执行结果，以一段连贯的自然语言返回给用户
- FR-1.4 意图模糊或缺少关键信息（如日程缺时间）时，向用户反问澄清，而不是猜测

**FR-2 日程 Agent**
- FR-2.1 `add_event`：新增事件（标题、日期时间、备注）；须将"明天下午三点"等相对时间解析为绝对时间
- FR-2.2 `list_events`：按时间范围查询（今天/本周/某日期）
- FR-2.3 `delete_event`：按标题模糊匹配删除

**FR-3 待办 Agent**
- FR-3.1 `add_todo`：新增待办（内容、可选截止时间）
- FR-3.2 `list_todos`：列出未完成待办
- FR-3.3 `complete_todo`：标记完成

**FR-4 记账 Agent**
- FR-4.1 `add_expense`：记录支出（金额、分类、备注、日期）；分类须在预设类目内（餐饮/交通/购物/居住/娱乐/其他），由 Agent 自动归类
- FR-4.2 `expense_summary`：按月 + 分类汇总，返回总金额与明细
- FR-4.3 一句输入含多笔开销时，拆分为多笔记录（S4）

**FR-5 笔记 Agent**
- FR-5.1 `add_note`：保存笔记（内容、可选标签）
- FR-5.2 `search_notes`：按关键词/标签检索

**FR-6 数据持久化**
- FR-6.1 每个领域一个 JSON 文件：`data/events.json` / `todos.json` / `expenses.json` / `notes.json`
- FR-6.2 所有写入操作落盘后才算成功；UTF-8、中文不转义（`ensure_ascii=False`）
- FR-6.3 工具函数为纯 Python，不依赖 LLM，可独立单元测试

**FR-7 CLI 交互**
- FR-7.1 启动后进入循环：读入一行 → 执行 → 打印结果
- FR-7.2 输入 `quit` / `exit` 退出
- FR-7.3 每次执行展示分派过程（verbose），便于验证"多 Agent 协作"这一核心卖点

### 4.3 非功能需求

- **NFR-1 可验证性**：工具的确定性逻辑（时间解析、金额汇总）必须能脱离 LLM 单独测试
- **NFR-2 成本控制**：单次指令 LLM 调用次数 ≤ 6 次（DeepSeek 单价低，但仍应避免 Agent 间冗余对话）
- **NFR-3 隐私**：所有数据仅存本地；API key 只放 `.env`，不入库、不进 git
- **NFR-4 可演进**：存储层与工具层解耦，未来可平滑替换 SQLite；Agent 定义与工具实现解耦，新增领域 Agent 不改现有代码

---

## 5. 技术方案要点

| 决策 | 结论 | 理由（来自前期调研） |
| --- | --- | --- |
| 框架 | CrewAI | 角色隐喻与"专职助手团队"一一对应；上手最快、代码量最少；Hierarchical 模式原生支持 Manager 自动委派 |
| LLM | DeepSeek（`deepseek-chat`） | 国内直连、成本低、中文好、工具调用能力足够 |
| 模型接入 | CrewAI 的 `LLM(model="deepseek/deepseek-chat")`，key 走 `DEEPSEEK_API_KEY` 环境变量 | CrewAI 底层走 litellm，原生支持 DeepSeek |
| 存储 | 本地 JSON 文件 | 原型期最简单可靠的方案；读写逻辑收敛在 `storage.py` |
| 界面 | 命令行 REPL | 最小可用界面，验证价值优先 |

### 目录结构

```
多agent个人管理工具/
├── PRD.md
├── README.md
├── requirements.txt     # crewai, python-dotenv
├── .env.example         # DEEPSEEK_API_KEY=your_key_here
├── storage.py           # JSON 读写
├── tools.py             # 4 个领域的工具函数（@tool 装饰器）
├── agents.py            # 4 个专职 Agent + Manager + Crew 定义
├── main.py              # CLI 入口
└── data/                # 运行时生成
```

---

## 6. 里程碑

| 阶段 | 内容 | 验收标准 |
| --- | --- | --- |
| M1 地基 | storage.py + tools.py | 工具函数脱离 LLM 单测通过（时间解析、汇总计算正确） |
| M2 组装 | agents.py + main.py | 程序可启动，Crew 可 kickoff |
| M3 端到端 | 真实指令验证 | 覆盖 S1–S7 全部场景，分派准确率达标 |
| M4（后续） | 提醒推送、SQLite、Web UI | 另立 PRD |

---

## 7. 风险与对策

| 风险 | 影响 | 对策 |
| --- | --- | --- |
| Manager 分派错误（把记账派给日程 Agent） | 核心体验崩塌 | S1–S7 测试集回归；专职 Agent 的 backstory 写清边界；失败案例迭代提示词 |
| 相对时间解析错误（"明天"到底哪天） | 日程记错 | 工具调用时把当前日期时间注入上下文；Agent 回复中复述解析结果供用户确认 |
| Hierarchical 模式 token 消耗超预期 | 成本上升 | verbose 观察调用链；必要时降级为 Sequential + 显式路由 |
| DeepSeek 工具调用格式不稳定 | 执行失败 | 选用 `deepseek-chat` 最新版；关键工具参数在描述中写明格式约束 |
| 范围蔓延（想加提醒、同步、UI） | 原型难产 | 非目标清单（2.3）即护栏，新需求进 M4 再说 |

---

## 8. 开放问题

1. 记账分类是固定 6 类还是允许用户自定义？（本期：固定 6 类，观察真实使用再定）
2. 删除/修改类操作是否需要二次确认？（本期：删除需要，修改不需要）
3. 是否需要对话记忆（上下文多轮）？（本期：单轮为主，CrewAI memory 暂不开启，避免不可控）
